# Supabase Configuration
# These variables are exposed to the client (VITE_ prefix)

# Your Supabase project URL (from Supabase Dashboard → Settings → API)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon key (from Supabase Dashboard → Settings → API)
# This is safe to expose - it only allows operations defined by RLS policies
VITE_SUPABASE_ANON_KEY=your-anon-key

# API Configuration
# Used for the Vercel serverless function (set in Vercel Dashboard)
# Do not commit actual values - use .env.local for overrides
