the known bugs of this project:

on mobile: the hamburguer menu has alot of items so we cannot acces the final elements, this should be resolved.

- touch controls are erratic on the brick breaker and teris game.
- we should introduce a better way to indicate the controls when playing on mobile.  for every game.
- the brick breaker game ui breaks on mobile
- the memory game does not shows the board correctly on mobile.
- when selecting the option on the menu and the component is loaded, the menu should close on mobile.
- the tower defense, the enemies do not move, posible solution adjust speed to cells per frames can solve the movement problem, instead of cells/second.